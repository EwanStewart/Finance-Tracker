name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: [self-hosted, photobox]
    steps:
      - name: Run deploy script
        run: /home/ewastewa/finance-tracker/deploy/update.sh
